<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/windowBackgroundColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="ProfileCircleImageStyle">
        <item name="civ_border_color">@color/border_color</item>
        <item name="civ_border_width">@dimen/profile_circle_image_border_width</item>
    </style>

    <style name="ProfileNameStyle">
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:textSize">@dimen/standard_font_size_large</item>
    </style>

    <style name="ProfileDetailsTextStyle">
        <item name="android:textColor">@color/secondary_text</item>
        <item name="android:textSize">@dimen/standard_font_size_normal</item>
    </style>

    <style name="ProfileDetailsTextStyle.ProfilePersonPostStyle">
        <item name="android:layout_marginTop">@dimen/small_margin</item>
        <item name="android:layout_marginBottom">@dimen/small_margin</item>
    </style>

    <style name="ProfileDetailsTextStyle.ProfileEmailStyle">
        <item name="android:textIsSelectable">true</item>
    </style>
</resources>
